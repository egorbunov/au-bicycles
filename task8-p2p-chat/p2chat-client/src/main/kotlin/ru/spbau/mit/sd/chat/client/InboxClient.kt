package ru.spbau.mit.sd.chat.client

/**
 * Part of a client, which takes care of accepting connections from other peers and
 * getting messages from them
 */
class InboxClient(val user: Void) {
    fun start() {}
    fun stop() {}
}
